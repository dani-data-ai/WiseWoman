{
  "name": "devotional-app",
  "version": "1.0.0",
  "description": "21-day devotional React app (Romanian only), mobile-optimized, Netlify-ready",
  "scripts": {
    "start": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  }
}